<div class="container p-4">
    <div class="row">
        <div class="col-md-8 mx-auto">
            <div class="card text-center">
                <div class="card-header" >
                    <b class="fs-3">Login</b3>
                </div>
                <div class="card-body" >
                    <img src="/img/diente.png" alt="Logo Tickets" class="card-img-top mx-auto m-2 w-50">
                    <form action="/signin" method="POST" name="form_login">
                        <div class="form-group p-2">
                            <input type="text"id="login_user" name="usuario_s" placeholder="Ingresa el usuario" class="form-control"
                            oninput="this.value = this.value.replace(/[^a-zA-Z\u00f1\u00d1\u00E0-\u00FC.]/g, '').replace(/(\..*)\./g, '$1');">
                        </div>
                        <div class="form-group p-2">
                            <input type="password" id="login_pass" name="clave_s" placeholder="Ingresa la contraseña"
                                class="form-control">
                        </div>
                        <div class="mb-2">
                            <div class="d-grid gap-2 col-10 mx-auto">
                                <button class="btn btn-success">
                                    <i class="bi bi-box-arrow-in-right"></i>
                                    Singin
                                </button>
                            </div>
                        </div>
                    </form>

                </div>
            </div>
        </div>
    </div>
</div>